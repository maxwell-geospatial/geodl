% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/makeChipsMultiClass.R
\name{makeChipsMultiClass}
\alias{makeChipsMultiClass}
\title{makeChipsMultiClass}
\usage{
makeChipsMultiClass(
  image,
  mask,
  n_channels = 3,
  size = 256,
  stride_x = 256,
  stride_y = 256,
  outDir,
  useExistingDir = FALSE
)
}
\arguments{
\item{image}{SpatRaster object or path to input image.}

\item{mask}{SpatRaster object or path to single-band mask.}

\item{n_channels}{Number of channels in the input image. Default is 3.}

\item{size}{Size of image chips in pixels. Default is 256.}

\item{stride_x}{Stride in the x (columns) direction. Default is 256.}

\item{stride_y}{Stride in the y (rows) direction. Default is 256.}

\item{outDir}{Full or relative path to the output directory. You must include
the final forward slash (e.g., "C:/data/chips/").}

\item{useExistingDir}{TRUE or FALSE. Write into an existing directory.
Default is FALSE.}
}
\value{
Image and mask files written to disk in TIFF format. No R object is returned.
}
\description{
Deprecated. Use \code{\link{makeChips}} with \code{mode = "Positive"} instead.
}
\details{
This function is a deprecated wrapper around \code{\link{makeChips}}. It
will be removed in a future version. Replace calls of the form:

\code{makeChipsMultiClass(image, mask, ...)}

with:

\code{makeChips(image, mask, ..., mode = "Positive")}
}
\examples{
\dontrun{
# Deprecated — use makeChips() instead:
makeChips(image = "INPUT IMAGE NAME AND PATH",
          mask = "INPUT RASTER MASK NAME AND PATH",
          n_channels = 3,
          size = 512,
          stride_x = 512,
          stride_y = 512,
          outDir = "DIRECTORY IN WHICH TO SAVE CHIPS",
          mode = "Positive",
          useExistingDir = FALSE)
}
}
